# URL base del backend (FastAPI). Cuando el backend esté disponible,
# cambiar VITE_USE_MOCK a "false" y apuntar VITE_API_URL al backend real.
VITE_API_URL=http://localhost:8000/api
VITE_USE_MOCK=true
